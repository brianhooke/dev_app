# Django Settings
# Copy this file to .env and fill in your actual values
# .env is gitignored and will never be committed

# Email Configuration (Office 365)
EMAIL_HOST_USER=purchase_orders@mason.build
EMAIL_HOST_PASSWORD=your_password_here
DEFAULT_FROM_EMAIL=purchase_orders@mason.build
EMAIL_CC=brian.hooke@mason.build

# Database (if using PostgreSQL locally)
# RDS_HOSTNAME=localhost
# RDS_DB_NAME=devappdb
# RDS_USERNAME=devappmaster
# RDS_PASSWORD=yourpassword
# RDS_PORT=5432

# Xero API (optional for local dev)
# XERO_CLIENT_ID=your_client_id
# XERO_CLIENT_SECRET=your_client_secret
# XERO_ENCRYPTION_KEY=your_encryption_key
